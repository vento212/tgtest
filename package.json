{
  "name": "tg-market-fullstack",
  "version": "1.0.0",
  "description": "Fullstack TON marketplace application",
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "build": "cd backend && npm install",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
